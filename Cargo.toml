[package]
name = "hc"
version = "0.2.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "3.1.1"
reqwest = "0.13"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "signal"] }
